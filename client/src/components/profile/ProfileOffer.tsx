import React from "react";
import { IOffer } from "../../models/models";

export const ProfileOffer = (offer: IOffer) => {
  console.log(offer);

  return (
    <div>
      <div>
        <p>{"p"}</p>
      </div>
    </div>
  );
};
